---
interface Props {
  platform: string;
  username: string;
}
const { platform, username } = Astro.props;
---

<a
  class="px-2 py-1 text-white bg-purple-800 no-underline"
  href={`https://www.${platform}.com/${username}`}>{platform}</a
>
